package com.davidshibru.taskflow.core.network

import org.junit.Assert.assertEquals
import org.junit.Test

class NetworkTest {
    @Test
    fun `initial test`() {
        assertEquals("Test should pass", 4, 2 + 2)
    }
}